import { TypeColorDirective } from './type-color.directive';

describe('TypeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeColorDirective();
    expect(directive).toBeTruthy();
  });
});
