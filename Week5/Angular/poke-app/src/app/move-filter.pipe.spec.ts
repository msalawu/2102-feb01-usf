import { MoveFilterPipe } from './move-filter.pipe';

describe('MoveFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MoveFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
